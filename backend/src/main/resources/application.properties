# ===============================
# APP INFO
# ===============================
spring.application.name=ai-health-habit-tracker

# ===============================
# DATABASE (MySQL)
# ===============================
spring.datasource.url=jdbc:mysql://localhost:3306/healthhabitdb?useSSL=false&serverTimezone=UTC
spring.datasource.username=habituser
spring.datasource.password=habit123
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ===============================
# JPA / HIBERNATE
# ===============================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.show-sql=true

# ===============================
# SUPABASE JWT SECRET
# (copy from Supabase ? Project Settings ? API ? JWT Secret)
# ===============================
supabase.jwt.secret=75PAFXLSGuLEuMdll0NYAOlg1vcvFd6l35nLQ2+173/Acygewmnjoar0gwZqnvDHxv39wxmtpogI/SSnKBBZbA==

# ===============================
# CORS / FRONTEND CONFIG
# ===============================
# (optional but useful for local React app)
spring.web.cors.allowed-origins=http://localhost:3000
spring.web.cors.allowed-methods=GET,POST,PUT,PATCH,DELETE,OPTIONS
spring.web.cors.allowed-headers=Authorization,Content-Type
